/**
 * Uptime Kuma driver module
 *
 * Registers the UptimeKumaDriver with the driver factory so it can be
 * selected via the `uptimekuma` driver type string.
 */

export { UptimeKumaDriver } from "./UptimeKumaDriver"
